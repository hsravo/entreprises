<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <%= link_to "Entreprise-Gouv", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <% if charity_signed_in? %>
                <li><%= link_to "Profil", root_path, class: "nav-link" %></li>
                <li><%= link_to "Se déconnecter", destroy_charity_session_path, method: :delete, class: "nav-link" %></li> 
                <% else %>
                <li class="nav-item">
                    <a class="nav-link" href="<%= new_charity_registration_path %>">Inscription</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="<%= new_charity_session_path %>">Se connecter</a></li>
                </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>